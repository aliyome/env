# js
set PATH /usr/local/bin $PATH
set PATH /Users/aliyome/.yarn/bin $PATH

# dlang
# set PATH /opt/ldc2-1.25.0-osx-arm64/bin $PATH

# opts
set PATH /opt/bin $PATH

# .net
# set PATH /Users/aliyome/.dotnet $PATH
# ln -s /usr/local/share/dotnet/dotnet /usr/local/bin

# homebrew
set -gx HOMEBREW_PREFIX "/opt/homebrew";
set -gx HOMEBREW_CELLAR "/opt/homebrew/Cellar";
set -gx HOMEBREW_REPOSITORY "/opt/homebrew";
set -q PATH; or set PATH ''; set -gx PATH "/opt/homebrew/bin" "/opt/homebrew/sbin" $PATH;
set -q MANPATH; or set MANPATH ''; set -gx MANPATH "/opt/homebrew/share/man" $MANPATH;
set -q INFOPATH; or set INFOPATH ''; set -gx INFOPATH "/opt/homebrew/share/info" $INFOPATH;

# pyenv (generated by `pyenv init -`)
set -gx PATH '/Users/aliyome/.pyenv/shims' $PATH
set -gx PYENV_SHELL fish
source '/opt/homebrew/Cellar/pyenv/1.2.24.1/libexec/../completions/pyenv.fish'
command pyenv rehash 2>/dev/null
function pyenv
  set command $argv[1]
  set -e argv[1]

  switch "$command"
  case rehash shell
    source (pyenv "sh-$command" $argv|psub)
  case '*'
    command pyenv "$command" $argv
  end
end

# ruby
set -gx $HOME/.rbenv/bin $PATH
